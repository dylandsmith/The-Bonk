<div class="welcome-grid">
  <section class="welcome-grid-title">
    <% if Current.user %>
      <section class="header-grid">
        <div class="welcome-search">
          <%= render 'movies/search_movies' %>
        </div>
        <div class="welcome-recommendation">
          <%= button_to 'Save Recommendation', movies_path, method: :post, class: 'recommendation-btn' %>
        </div>
      </section>
    <% end %>
  </section>
  <section class="welcome-main">
    <%= form_with model: @movie do |form| %>
      <div>
        <%#= form.label :title, class: 'movie-form' %>
        <div class="movie-form">
          <%= form.text_field :title, placeholder: 'Movie Title', class: 'movie-form' %>
        </div>
      </div>
      <div>
        <%= form.label :author %>
        <%= form.text_field :author %>
      </div>
      <%= form.submit %>
    <% end %>
  </section>
  <section class="user_info">
    <%= render 'shared/user_panel' %>
  </section>
</div>